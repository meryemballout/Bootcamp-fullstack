// Exercise 2: Conditional (Ternary) Operator
// Use the conditional (ternary) operator to write a function that returns
const winBattle = () => true;
// 10 if the function winBattle() returns true, and 1 if it returns false.
let experiencePoints = winBattle() ? 10 : 1;
console.log(experiencePoints);
